[package]
name = "vellum-markdown-lint"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
vellum-extension-sdk = { path = "../../crates/extension-sdk" }
serde = { version = "1", features = ["derive"] }
