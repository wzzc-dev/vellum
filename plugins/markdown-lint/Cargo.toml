[package]
name = "vellum-markdown-lint"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
vellum-plugin-sdk = { path = "../../crates/plugin-sdk" }
postcard = { version = "1", features = ["alloc"] }
serde = { version = "1", features = ["derive"] }
