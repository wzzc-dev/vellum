# Legacy Extension Examples

These examples use the compatibility-only `vellum:extension/extension-world` protocol.

They remain useful for regression testing the old extension host, but new GUI framework work should target `examples/plugins` and the root `wit/vellum-app.wit` protocol.
