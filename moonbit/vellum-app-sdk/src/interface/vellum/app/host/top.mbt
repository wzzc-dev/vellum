// Generated by `wit-bindgen` 0.57.1. DO NOT EDIT!
///|
pub fn log(level : @types.LogLevel, message : String) -> Unit {

      let ptr = mbt_ffi_str2ptr(message)
      wasmImportLog(level.ordinal(), ptr, message.length());
      mbt_ffi_free(ptr)

}
///|
pub fn show_status_message(message : String) -> Result[Unit, @types.AppError] {

      let ptr = mbt_ffi_str2ptr(message)
      let return_area = mbt_ffi_malloc(12)
      wasmImportShowStatusMessage(ptr, message.length(), return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.AppError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(ptr)
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn request_render() -> Result[Unit, @types.AppError] {

      let return_area = mbt_ffi_malloc(12)
      wasmImportRequestRender(return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.AppError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn editor_command(command_id : String, payload : Array[@types.Property]) -> Result[Unit, @types.AppError] {
      let cleanup_list : Array[Int] = []

      let ptr = mbt_ffi_str2ptr(command_id)

      let address = mbt_ffi_malloc((payload).length() * 16);
      for index = 0; index < (payload).length(); index = index + 1 {
            let iter_elem : @types.Property = (payload)[(index)]
            let iter_base = address + (index * 16);

            let ptr0 = mbt_ffi_str2ptr((iter_elem).name)
            mbt_ffi_store32((iter_base) + 4, (iter_elem).name.length())
            mbt_ffi_store32((iter_base) + 0, ptr0)

            let ptr1 = mbt_ffi_str2ptr((iter_elem).value)
            mbt_ffi_store32((iter_base) + 12, (iter_elem).value.length())
            mbt_ffi_store32((iter_base) + 8, ptr1)
            cleanup_list.push(ptr0)
            cleanup_list.push(ptr1)

      }
      let return_area = mbt_ffi_malloc(12)
      wasmImportEditorCommand(ptr, command_id.length(), address, (payload).length(), return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.AppError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(ptr)
      mbt_ffi_free(address)
      mbt_ffi_free(return_area)

      cleanup_list.each(mbt_ffi_free)
      return ret

}
///|
pub fn get_editor_snapshot() -> Result[@types.EditorSnapshot, @types.AppError] {

      let return_area = mbt_ffi_malloc(48)
      wasmImportGetEditorSnapshot(return_area);

      let lifted4 = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  let lifted : String? = match mbt_ffi_load8_u((return_area) + 12) {
                        0 => Option::None
                        1 => {

                              let result0 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 16), mbt_ffi_load32((return_area) + 20))

                              Option::Some(result0)
                        }
                        _ => panic()
                  }

                  let result1 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 32), mbt_ffi_load32((return_area) + 36))

                  let result2 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 40), mbt_ffi_load32((return_area) + 44))

                  Result::Ok(@types.EditorSnapshot::{display_name : result, path : lifted, dirty : (mbt_ffi_load8_u((return_area) + 24) != 0), word_count : (mbt_ffi_load32((return_area) + 28)).reinterpret_as_uint(), document_text : result1, view_mode : result2})
            }
            1 => {

                  let result3 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.AppError::{message : result3})
            }
            _ => panic()
      }
      let ret = lifted4
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn plugin_list() -> Result[Array[@types.PluginInfo], @types.AppError] {

      let return_area = mbt_ffi_malloc(12)
      wasmImportPluginList(return_area);

      let lifted13 = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  let array10 : Array[@types.PluginInfo] = [];
                  for index11 = 0; index11 < (mbt_ffi_load32((return_area) + 8)); index11 = index11 + 1 {
                        let iter_base = (mbt_ffi_load32((return_area) + 4)) + (index11 * 64)

                        let result = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 0), mbt_ffi_load32((iter_base) + 4))

                        let result0 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                        let result1 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 16), mbt_ffi_load32((iter_base) + 20))

                        let result2 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 24), mbt_ffi_load32((iter_base) + 28))

                        let array : Array[@types.PluginCommand] = [];
                        for index = 0; index < (mbt_ffi_load32((iter_base) + 40)); index = index + 1 {
                              let iter_base = (mbt_ffi_load32((iter_base) + 36)) + (index * 16)

                              let result3 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 0), mbt_ffi_load32((iter_base) + 4))

                              let result4 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                              array.push(@types.PluginCommand::{id : result3, title : result4})
                        }
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 36))

                        let array7 : Array[@types.PluginPanel] = [];
                        for index8 = 0; index8 < (mbt_ffi_load32((iter_base) + 48)); index8 = index8 + 1 {
                              let iter_base = (mbt_ffi_load32((iter_base) + 44)) + (index8 * 16)

                              let result5 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 0), mbt_ffi_load32((iter_base) + 4))

                              let result6 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                              array7.push(@types.PluginPanel::{id : result5, title : result6})
                        }
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 44))

                        let lifted : String? = match mbt_ffi_load8_u((iter_base) + 52) {
                              0 => Option::None
                              1 => {

                                    let result9 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 56), mbt_ffi_load32((iter_base) + 60))

                                    Option::Some(result9)
                              }
                              _ => panic()
                        }

                        array10.push(@types.PluginInfo::{id : result, name : result0, version : result1, description : result2, state : @types.PluginState::from(mbt_ffi_load8_u((iter_base) + 32)), commands : array, panels : array7, error : lifted})
                  }
                  mbt_ffi_free(mbt_ffi_load32((return_area) + 4))

                  Result::Ok(array10)
            }
            1 => {

                  let result12 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.AppError::{message : result12})
            }
            _ => panic()
      }
      let ret = lifted13
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn plugin_enable(id : String) -> Result[Unit, @types.AppError] {

      let ptr = mbt_ffi_str2ptr(id)
      let return_area = mbt_ffi_malloc(12)
      wasmImportPluginEnable(ptr, id.length(), return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.AppError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(ptr)
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn plugin_disable(id : String) -> Result[Unit, @types.AppError] {

      let ptr = mbt_ffi_str2ptr(id)
      let return_area = mbt_ffi_malloc(12)
      wasmImportPluginDisable(ptr, id.length(), return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.AppError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(ptr)
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn plugin_reload(id : String) -> Result[Unit, @types.AppError] {

      let ptr = mbt_ffi_str2ptr(id)
      let return_area = mbt_ffi_malloc(12)
      wasmImportPluginReload(ptr, id.length(), return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.AppError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(ptr)
      mbt_ffi_free(return_area)
      return ret

}

