// Generated by `wit-bindgen` 0.57.1. DO NOT EDIT!
///|
pub fn document_text() -> Result[String, @types.HostError] {

      let return_area = mbt_ffi_malloc(12)
      wasmImportDocumentText(return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Ok(result)
            }
            1 => {

                  let result0 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.HostError::{message : result0})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn document_path() -> Result[String?, @types.HostError] {

      let return_area = mbt_ffi_malloc(16)
      wasmImportDocumentPath(return_area);

      let lifted1 = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  let lifted : String? = match mbt_ffi_load8_u((return_area) + 4) {
                        0 => Option::None
                        1 => {

                              let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 8), mbt_ffi_load32((return_area) + 12))

                              Option::Some(result)
                        }
                        _ => panic()
                  }

                  Result::Ok(lifted)
            }
            1 => {

                  let result0 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.HostError::{message : result0})
            }
            _ => panic()
      }
      let ret = lifted1
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn replace_range(start : UInt64, end : UInt64, text : String) -> Result[Unit, @types.HostError] {

      let ptr = mbt_ffi_str2ptr(text)
      let return_area = mbt_ffi_malloc(12)
      wasmImportReplaceRange((start).reinterpret_as_int64(), (end).reinterpret_as_int64(), ptr, text.length(), return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.HostError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(ptr)
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn insert_text(position : UInt64, text : String) -> Result[Unit, @types.HostError] {

      let ptr = mbt_ffi_str2ptr(text)
      let return_area = mbt_ffi_malloc(12)
      wasmImportInsertText((position).reinterpret_as_int64(), ptr, text.length(), return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.HostError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(ptr)
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn set_decorations(data : FixedArray[Byte]) -> Result[Unit, @types.HostError] {

      let ptr = mbt_ffi_bytes2ptr(data)

      let return_area = mbt_ffi_malloc(12)
      wasmImportSetDecorations(ptr, data.length(), return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.HostError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(ptr)
      mbt_ffi_free(return_area)
      return ret

}
///|
pub fn clear_decorations() -> Result[Unit, @types.HostError] {

      let return_area = mbt_ffi_malloc(12)
      wasmImportClearDecorations(return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  Result::Err(@types.HostError::{message : result})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(return_area)
      return ret

}

